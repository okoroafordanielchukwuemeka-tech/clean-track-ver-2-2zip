- [Event & Notification Engine](event-notification-engine.md) — emitEvent() fires async (non-blocking) from route handlers; notifications table uses laundryId scoping
- [Expenditures & Profitability](expenditures-profitability.md) — EXPENSE_CATEGORIES const exported from schema; profit = collectedRevenue - totalExpenses; requireOwner guards write endpoints
- [UI Components Available](ui-components.md) — shadcn/ui components are hand-built; always check artifacts/clean-track/src/components/ui/ before importing; alert-dialog was missing and had to be created manually from @radix-ui/react-alert-dialog
- [SLA & Urgency System](sla-urgency.md) — processingDueAt stored at order creation from laundry SLA settings; frontend falls back to createdAt + defaults for old orders; 4 urgency levels: safe/attention/urgent/overdue
- [Branch Isolation & Analytics Auth](branch-isolation.md) — all ID-based routes enforce branchId; analytics uses requireAuth (not requireOwner) so workers get branch-scoped data via getEffectiveBranchId()
- [Demo Environment](demo-environment.md) — seed script at scripts/seed-demo.ts; owner: demo@cleantrack.ng / Demo@1234; 5 branches, 20 workers, 200 customers, 1000 orders
- [Receipt Number Generation](receipt-number-generation.md) — generateReceiptNumber uses sql.raw() for SUBSTRING FROM position; plain ${n} in Drizzle sql template silently fails the integer cast
- [Branch Filtering Pattern](branch-filtering-pattern.md) — frontend pages must pass activeBranchId from useBranch() into queryKey AND queryFn; api.ts functions accept branchId? param; /orders/recent was missing backend route (added)
- [Customer Statement Feature](customer-statement.md) — GET /customers/:id/statement returns chronological ledger with running balance; all 5 entry types; credit balance shows negative closingBalance; frontend Statement tab with print-to-PDF via window.open
- [Orders Pagination](orders-pagination.md) — GET /orders default limit is 500 (was 50); all 1000 orders tested with 0 financial errors; branch filtering adds branchId condition via getEffectiveBranchId()
- [Receipt Endpoint Consistency](receipt-endpoint-consistency.md) — GET /receipts/:num includes branch object; GET /orders/:id/receipt also must include branch + cashierName; order sub-object in both must include branchId
- [Phase 2 Read Cache](phase2-read-cache.md) — persistQueryClient() called at module scope (not PersistQueryClientProvider which breaks with React strict-mode); useCachedQuery uses type intersection not interface extends for UseQueryResult
- [Offline Status Updates](offline-status-updates.md) — enqueueOrderStatusUpdate uses "srv-NNN" synthetic localId for server-synced orders; Pass 3 in processQueue; recovery rebuilds from pending_status_update orders
- [Offline Pickup Sync](offline-pickup-sync.md) — Pass 5 in processQueue; Passes 3+4 must add to doneLocalIds on success or same-cycle pickup deps never resolve; items store server IDs as String(orderItemId) in orderItemLocalId field
- [Sync Engine Error Handling](sync-engine-error-handling.md) — HttpError class in api.ts carries HTTP status; 4xx (excl 408/429) = immediate permanent failure; backoff = 2^attempts*1000ms capped at 300s (5min); MAX_TRANSIENT_ATTEMPTS=20; requeueFailedEntry/requeueAllFailed exported; SyncFailedPanel in layout
- [Status Sync Last-Write-Wins](status-sync-lww.md) — Pass 3 deduplicates update_order_status by localId before sending; stale entries (all but latest per order) marked done without server call; deduped array iterates position-sorted pending list
- [Payment Financial Safety Layer](payment-financial-safety.md) — FinancialConflictError + ConflictCode in queue-service; validatePaymentPreSync() GETs order before POST; conflict path sets syncStatus="conflict" on LocalPayment; syncLog prefixed "CONFLICT:<code>:"; API prices are strings — always parseFloat(String(...))
- [Payment Sync Integration](payment-sync-integration.md) — syncEngine.notifyPaymentSynced() emits item_synced after Pass 4 success; order-detail subscribes and invalidates RQ caches; ConflictSyncBadge (red) shown for syncStatus="conflict"; auth endpoint is /api/auth/owner-login; health endpoint is /api/healthz
- [Pickup Sync Integration](pickup-sync-integration.md) — PickupConflictError + validatePickupPreSync before POST; notifyPickupSynced emits item_synced; order-detail dual-subscriber (payment+pickup); idempotency middleware must await DB insert before flushing response
- [Worker Permissions Enforcement](worker-permissions-enforcement.md) — checkPermission BEFORE idempotencyMiddleware on all mutating routes; 8 perm fields in JWT; owners bypass; workers denied by default if no permissions in JWT
- [Sync Telemetry & Fleet Visibility](sync-telemetry.md) — device_heartbeats table; POST /api/telemetry/heartbeat (requireAuth); GET /api/operations/sync-health (requireOwner); Sync Health tab in Operations Center; initTelemetry() called after runRecovery().finally()
- [Backup & DR Audit Implementation](backup-dr-audit.md) — schema_snapshots table (22nd table); 5 new /api/recovery/* endpoints; Backup Now + Verify buttons in DR panel; Runbook tab in Operations Center; postgresql nix dep installed for pg_dump
- [Schema Versioning & Client Compatibility](schema-versioning.md) — APP_VERSION + SCHEMA_VERSION in lib/version.ts; X-Client-Version sent on every request; X-Min-Client-Version response header drives OutdatedClientBanner; schemaVersion stamped on SyncQueueEntry; Dexie v2 migration is same-schema (schemaVersion is optional, no data loss)
- [Alert Engine & Incident Management](alert-engine.md) — alerts table (23rd); fingerprint dedup; 11 rules; auto-resolve via system; runs every 5min on startup + scheduler; POST /api/alerts/run-check for manual; requireOwner on all alert routes
- [Admin Command Center](admin-command-center.md) — platform_admins table (24th); JWT type="admin"; /api/admin/* routes; 5 tabs: overview/tenants/devices/storage/backups; pg_class + c.oid for table sizes (pg_stat_user_tables.tablename breaks in Drizzle sql template)
- [WhatsApp Provider Integration](whatsapp-provider.md) — provider_configs table (25th); ChannelProvider interface; WhatsAppCloudProvider uses Node 20 native fetch; providerRegistry has 5min TTL cache; webhooks PUBLIC route registered before requireAuth in index.ts; normalizePhoneE164 handles Nigerian local (08xx) format; access token masked in GET response (last 4 chars shown)
- [Subscription & Trial System](subscription-trial.md) — Phase B complete; subscription_logs table (26th); requireOperational blocks suspended/cancelled; requireEntitlement("HAS_WHATSAPP") on communication send routes; admin /subscriptions tab with state-transition POST endpoint; dashboard SubscriptionBanner; GET /subscription/status for owners
- [Plan Limit Enforcement](plan-limit-enforcement.md) — usage-service.ts computes live DB truth; requirePlanLimit() on POST /orders|workers|branches; 12 usage alert rules in alert-engine; GET /subscription/usage; Settings Billing tab; admin TenantsTab usage bars + sort
- [Worker Operational Messaging](worker-operational-messaging.md) — 3 worker-accessible endpoints on /orders router; messages query JOINs notificationEvents to filter by orderId; Order.status type lacks "cancelled" so never compare against it
- [Pre-launch Hardening](pre-launch-hardening.md) — express-rate-limit v8 requires app.set("trust proxy",1) before limiters; SESSION_SECRET is a Replit Secret (not env var); BACKUP_SECRET stored as shared env var; api.health.production() added to api.ts
- [Worker Token Invalidation](worker-token-invalidation.md) — DB lookup required in requireAuth (async); pure-JWT iat vs pinChangedAt comparison is circular; authLimiter skips /me endpoint; owner passwordChangedAt check must floor to seconds (iat is whole seconds, DB timestamp has ms — equal-second tokens were rejected)
- [Trial Entitlements & Conversion UX](trial-conversion-ux.md) — TRIAL_FEATURE_TIER="pro" in entitlements.ts; trial gives Growth features via getEffectivePlanFeatures/Limits; /pricing page + /welcome post-signup screen; signup trial badge; welcome-viewed activation event
- [First 100 Users Readiness Fixes](readiness-audit.md) — 6 friction fixes: signup password checklist; sidebar 15→12+Advanced; checklist worker step optional; feedback button; services category Select; demo /demo-login endpoint with demoLimiter
- [Activation Analytics System](activation-analytics.md) — activation_events table (unique on laundry_id+event_name); 9 event types; scoring 0-100; 4 admin API endpoints; Growth tab in admin-command-center; email tracking pixel via GET /auth/email-track
- [Admin Security & Impersonation](admin-security.md) — admin_audit_log table (27th); role field on platform_admins (super_admin/support_admin/finance_admin); requireSuperAdmin middleware; impersonation via POST /admin/tenants/:id/impersonate; ImpersonationBanner in layout; admin login rate-limited (10/15min)
- [WhatsApp Embedded Signup](whatsapp-embedded-signup.md) — Phase 2 complete; FB SDK loaded dynamically only when META_APP_ID set; message event listener captures waba_id+phone_number_id before FB.login callback delivers code; manual form kept as fallback; 4 audit events wired; no schema changes needed
